{
  "name": "telltale",
  "version": "0.0.4",
  "private": false,
  "description": "Cost-aware island convergence for Claude Code work.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MTGVim/telltale.git"
  },
  "scripts": {
    "validate": "claude plugin validate . --strict && python3 scripts/telltalectl.py validate-schemas && python3 scripts/telltalectl.py smoke && python3 -m unittest discover -s tests -v"
  }
}
